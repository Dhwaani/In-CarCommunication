# Examples

This directory is for rendered audio demonstrating the suppressor — before and
after captures of the howling threshold.

Generate the files by yourself in this folder path:

```bash
faust2sndfile -I lib dsp/icc_loop_demo.dsp
./icc_loop_demo input.wav output.wav
```

An example demo of the audio files will be uploaded.
